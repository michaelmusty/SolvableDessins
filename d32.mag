// assumes current directory = repo directory
AttachSpec("spec");
AttachSpec("~/Dropbox/belyi/Code/spec");

repo := GetCurrentDirectory();

load "catalog16.mag";

sigma := d8t1_1;

SetProfile(true);
l0, l1, loo, all, arebelyimaps, justramified, covers := PossibleGaloisCoveringPermutationTriples(sigma : brutal := false, lax := false, return_everything := true);
SetProfile(false);
G := ProfileGraph();
ChangeDirectory("Profiler");
ProfileHTMLOutput(G, "profile");
ChangeDirectory(repo);
