AttachSpec("spec");
AttachSpec("~/Dropbox/belyi/Code/spec");

load "query.mag";

sigma := d8t1_2;

l0, l1, loo, all, arebelyimaps, justramified, covers :=
PossibleGaloisCoveringPermutationTriples(sigma : weakly := false);

l0w, l1w, loow, allw, arebelyimapsw, justramifiedw, coversw :=
PossibleGaloisCoveringPermutationTriples(sigma : weakly := true);
