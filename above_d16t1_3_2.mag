AttachSpec("spec");
AttachSpec("~/Dropbox/belyi/Code/spec");

load "catalog16.mag";

sigma := p16t1_3[2];

l0, l1, loo, all, arebelyimaps, justramified, covers := PossibleGaloisCoveringPermutationTriples(sigma : brutal := false, lax := true, return_everything := true);
